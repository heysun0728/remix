pragma lity ^1.2.3;
contract RandNumberExample {
  uint randnum;
  uint test;
  constructor() public {
    randnum = rand() + test;
    bool val = isValidator(msg.sender);
    safeuint a = 2000;
    safeuint b = a * 12345;
  }

  function getAndSetRandnum() public returns (uint) {
    return randnum;
  }

  function c(uint a) public freegas returns (uint) {
    test = a;
    return test;
  }
}